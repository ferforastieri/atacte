export { prisma } from './client';
export { PrismaClient } from '@prisma/client';
export type {
  User,
  PasswordEntry,
  CustomField,
  UserSession,
  AuditLog,
  SecureNote,
  Prisma
} from '@prisma/client';

